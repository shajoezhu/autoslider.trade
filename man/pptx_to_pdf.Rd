% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pptx_to_pdf.R
\name{pptx_to_pdf}
\alias{pptx_to_pdf}
\title{Convert PowerPoint files to PDF}
\usage{
pptx_to_pdf(path, output_dir = NULL)
}
\arguments{
\item{path}{\code{character} vector of paths to \code{.pptx} files}

\item{output_dir}{\code{character} Directory to write the PDFs into. Defaults to
the directory of each input file.}
}
\value{
A \code{character} vector of the created PDF paths, invisibly
}
\description{
Rendered slide decks are easier to share as PDF. \code{pptx_to_pdf()} converts
one or more \code{.pptx} files, choosing automatically between the two
converters it can find: LibreOffice (\code{soffice --headless}) or Microsoft
PowerPoint through Windows COM automation. The latter is what makes the
function usable from WSL, where no Linux converter is installed but a
Windows Office is reachable.
}
\examples{
if (interactive()) {
  generate_slides(t_performance_slide(eg_prices), "performance.pptx")
  pptx_to_pdf("performance.pptx")
}

}
